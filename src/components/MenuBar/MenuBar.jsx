import "./MenuBar.css";

function MenuBar() {
    return (
        <section className="menu-bar">

            <a href="#">Home</a>
            <a href="#">Products</a>
            <a href="#">Categories</a>
            <a href="#">About</a>
            <a href="#">Contact</a>

        </section>
    );
}

export default MenuBar;